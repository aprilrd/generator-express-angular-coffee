language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-express-angular-coffee' ]; then cd .. && eval "mv $currentfolder generator-express-angular-coffee" && cd generator-express-angular-coffee; fi

